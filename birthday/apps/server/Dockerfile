# ── Placeholder Dockerfile — Colyseus realtime server ──
# The production build (workspace install, bundling, runtime command) is
# finalized when the server gains room logic in M3+. For now this only
# documents the intended base image.
FROM node:20-alpine
WORKDIR /app
# TODO(M3+): copy workspace, install with pnpm, build, and run the server.
CMD ["node", "--version"]
