import { LandingScreen } from "@/components/landing/LandingScreen";

/** Invitation landing page (M1). Replaces the temporary M0 foundation check. */
export default function Page() {
  return <LandingScreen />;
}
