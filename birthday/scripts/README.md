# Scripts

- `build-assets.mjs` — placeholder for the asset pipeline (atlas packing, audio
  encoding, image optimization) described in [ASSET_BIBLE.md](../ASSET_BIBLE.md).
  No-op until real art is added. Run via `pnpm assets:build`.
