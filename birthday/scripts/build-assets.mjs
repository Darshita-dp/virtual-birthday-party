/*
 * Placeholder asset build (atlas packing + audio encode + image optimization).
 * Implemented in a later milestone per ASSET_BIBLE.md. No-op for now.
 */
console.log(
  "[build-assets] placeholder — the asset pipeline lands with the first real art. Nothing to do yet.",
);
